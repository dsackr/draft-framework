schemaVersion: '1.0'
type: capability
aiHint: A Capability is a first-class architecture capability that an AI resolves to the
  company owner and approved Technology Component implementations before recommending
  a standard answer.
requiredFields:
- schemaVersion
- id
- type
- name
- description
- catalogStatus
- definitionOwner
- domain
- implementations
optionalFields:
- version
- tags
- owner
idPattern: ^capability\.[a-z0-9.-]+$
enumFields:
  catalogStatus:
  - stub
  - draft
  - approved
fieldTypes:
  definitionOwner: dict
  owner: dict
  tags: list
  implementations: list
collectionSchemas:
  implementations: implementation
fieldDescriptions:
  schemaVersion: Use the schema version string supported by this framework release, normally
    "1.0".
  id: Use a namespaced capability ID such as capability.log-management.
  type: Use capability.
  name: Use the plain-language capability name.
  description: Explain what architecture outcome this capability represents.
  catalogStatus: Use catalog maturity for the capability definition itself.
  definitionOwner: Identify the framework, provider, or company team accountable for
    maintaining the capability definition and vocabulary.
  owner: Identify the company team accountable for lifecycle decisions on this capability;
    required in the effective workspace when implementations are assigned.
  domain: Reference the owning domain object ID.
  implementations: List company-approved Technology Component implementations; framework base
    files must leave this empty because the framework does not make company adoption decisions.
implementation:
  requiredFields:
  - ref
  - lifecycleStatus
  optionalFields:
  - configuration
  - notes
  fieldDescriptions:
    ref: Reference a Technology Component approved to satisfy this capability.
    lifecycleStatus: 'Record the company disposition for this implementation: pre-invest,
      invest, maintain, disinvest, or exit.'
  enumFields:
    lifecycleStatus:
    - pre-invest
    - invest
    - maintain
    - disinvest
    - exit
